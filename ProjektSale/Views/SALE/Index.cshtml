@model IEnumerable<ProjektSale.SALE>

@{
    ViewBag.Title = "Sale";
}

<h2>Lista sal</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NAZWA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OPIS)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NAZWA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OPIS)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_SALI }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_SALI }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_SALI })
        </td>
    </tr>
}

</table>
