@model IEnumerable<ProjektSale.BIURA>

@{
    ViewBag.Title = "Biura";
}

<h2>Lista biur</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NAZWA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ADRES)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NAZWA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ADRES)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.ID_BIURA })  |
            @Html.ActionLink("Details", "Details", new { id = item.ID_BIURA }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.ID_BIURA })
        </td>
    </tr>
}

</table>
